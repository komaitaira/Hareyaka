<div class="container">
  <div class="row">
    <div class="col-md-3 col-lg-3">
      <%= render 'admin/shared/search_user', search: @search %>
    </div>

    <% if @q_users %>
      <div class="col-md-9 col-lg-9">
        <h4>会員一覧</h4>
        <% if @q_users.empty? %>
          <%# 検索該当なしの場合 %>
          <p class="alert alert-info" role="alert">条件に合致する記事が見つかりませんでした</p>
        <% else %>
          <%# 検索該当ありの場合 %>
          <p><%="全#{@q_users.total_count}件中" + "(#{@q_users.count}件表示)" %></p>
          <%= render 'user_list', users: @q_users %>
          <div class="pagination-col offset-sm-3 col-sm-6 offset-md-4 col-md-4 offset-lg-4 col-lg-4">
            <p class="text-center" >
              <%= paginate @q_users %>
            </p>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
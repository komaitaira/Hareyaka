<table class='table table-borderless'>
  <tbody>
    <% notifications.each do |notification| %>
      <tr>
        <td><%= attachment_image_tag notification.user, :profile_image, class: "img-fluid notification-face", fallback: "no_image.png" %></td>
        <td>
          <%= notification.sender_name %>さんからの<%= link_to 'メッセージ', corporate_room_path(notification.room_id) %>があります
          <%= " (#{time_ago_in_words(notification.created_at)}前)" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>